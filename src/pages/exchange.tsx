import React from "react";
import PageLayout from "../layouts/PageLayout";
import Hero from "../components/exchange/exchange"; // assuming you have this component

const Exchange = () => {
  return (
    <PageLayout>
      <Hero />
    </PageLayout>
  );
};

export default Exchange;
